{
  "name": "@jakeryderv/rocky",
  "version": "0.1.0",
  "description": "Rocky coding-agent platform: CLI on a forked Pi harness",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "bin": {
    "rocky": "dist/cli.js"
  },
  "files": [
    "dist",
    "npm-shrinkwrap.json",
    "docs",
    "README.md",
    "CHANGELOG.md",
    "LICENSE",
    "THIRD_PARTY_NOTICES.md"
  ],
  "scripts": {
    "dev": "npm run build && node --enable-source-maps dist/cli.js",
    "clean": "node scripts/clean.mjs",
    "build": "npm run clean && npm run build --workspace @jakeryderv/rocky-harness && tsc -p tsconfig.build.json && node -e \"require('node:fs').chmodSync('dist/cli.js', 0o755)\"",
    "start": "node --enable-source-maps dist/cli.js",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "check": "biome check .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "smoke": "npm run build && npm run smoke:built",
    "smoke:built": "node scripts/smoke.mjs",
    "verify": "npm run check && npm run build && npm run typecheck && npm run client:typecheck && npm run test:coverage && npm test --workspace @jakeryderv/rocky-harness && npm run smoke:built",
    "security:audit": "npm audit --omit=dev --audit-level=high",
    "client": "npm run build && bun scripts/client.mjs",
    "client:typecheck": "tsc -p packages/client/tsconfig.json --noEmit",
    "client:test": "bun test --cwd packages/client",
    "client:smoke": "bun scripts/client-smoke.mjs",
    "client:verify": "npm run client:typecheck && npm run client:test && npm run client:smoke",
    "client:nobuild": "bun scripts/client.mjs",
    "hooks:install": "git config core.hooksPath .githooks"
  },
  "dependencies": {
    "@jakeryderv/rocky-harness": "*"
  },
  "overrides": {
    "@earendil-works/pi-agent-core": "0.84.2",
    "@earendil-works/pi-ai": "0.84.2",
    "@earendil-works/pi-telemetry": "0.84.2",
    "@earendil-works/pi-tui": "0.84.2"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.10",
    "@types/node": "24.13.3",
    "@vitest/coverage-v8": "4.1.11",
    "tsx": "4.23.12",
    "typescript": "5.9.3",
    "vitest": "4.1.11"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jakeryderv/rocky.git"
  }
}
